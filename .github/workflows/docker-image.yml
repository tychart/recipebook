name: Build and deploy Docker image stack on self-hosted runner

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build and restart
        run: |
          docker compose down
          docker compose --env-file /home/gh-runner/.secrets/recipebook.env up -d --build
          docker image prune -f
