# Database
PG_USER=RecipeAdmin
PG_PASSWORD=R3c1peB00k


# S3 / RustFS
# When running via docker-compose, the backend runs in the recipebook-server container
# and must talk to the RustFS service by its Docker service name.
S3_ENDPOINT=http://recipebook-rustfs:9000
S3_BUCKET=recipe-images
S3_KEY=rustfsadmin
S3_SECRET=rustfsadmin